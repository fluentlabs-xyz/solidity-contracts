[profile.default]
cache_path = "forge-cache"
evm_version = "cancun"
libs = ["lib"]
optimizer = true
optimizer_runs = 200
out = "forge-out"
script = "scripts"
solc_version = "0.8.30"
src = "contracts"
test = "test"

[rpc_endpoints]
# Fork targets for dual-fork e2e tests
l1 = "https://ethereum-sepolia-rpc.publicnode.com"
l2 = "https://rpc.dev.fluent.xyz"

# Additional named endpoints (optional env-driven)
mainnet = "${MAINNET_RPC}"
sepoliaEth = "${RPC_URL_SEPOLIA_ETH}"
fluentTestnet = "${RPC_URL_FLUENT_TESTNET}"
fluentDev = "${RPC_URL_FLUENT_DEV}"

[etherscan]
sepolia = { key = "${ETHERSCAN_API_KEY}" }

[profile.coverage]
via_ir = true
